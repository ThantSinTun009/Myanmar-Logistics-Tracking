'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {getDemoState,getDemoUser} from '../../lib/demo'
export default function Trader(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{const load=()=>{const u=getDemoUser();const s=getDemoState();setRows(s.shipments.filter((r:any)=>r.trader_id===u?.id).sort((a:any,b:any)=>b.created_at.localeCompare(a.created_at)))};load();window.addEventListener('logistics-demo-update',load);return()=>window.removeEventListener('logistics-demo-update',load)},[]);return <><div className="top"><div><h1>Trader Dashboard</h1><p className="muted">Demo mode — local browser data.</p></div><Link className="btn" href="/trader/request">Create Shipment</Link></div><div className="grid grid2">{rows.map(r=><Link href={'/trader/shipments/'+r.id} className="card" key={r.id}><div className="row" style={{justifyContent:'space-between'}}><b>{r.tracking_number}</b><span className="badge">{r.status}</span></div><p>{r.origin} → {r.destination}</p><p className="muted">{r.cargo_description}</p></Link>)}</div></>}
